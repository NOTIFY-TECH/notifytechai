// CustomCursor.tsx
// -----------------
// This component disables the custom cursor
// and keeps the default browser arrow everywhere.

const CustomCursor = () => {
  // Returning null = no custom cursor, default arrow is used
  return null;
};

export default CustomCursor;
